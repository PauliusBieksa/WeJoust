using UnityEngine;

public class ItemSpawnPoint : MonoBehaviour
{
    public GameObject SpawnedMask;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
